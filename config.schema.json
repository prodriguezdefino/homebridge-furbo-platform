{
  "pluginAlias": "FurboPlatform",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "email": {
        "title": "Furbo Account Email",
        "type": "string",
        "required": true,
        "format": "email",
        "default": "dummy@email.com"
      },
      "encpass": {
        "title": "Furbo Account Encrypted Password",
        "type": "string",
        "required": true,
        "default": "somepassword"
      }
    }
  }
}